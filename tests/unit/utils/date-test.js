import date from 'frontend-ipdc-enrichment/utils/date';
import { module, test } from 'qunit';

module('Unit | Utility | date', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = date();
    assert.ok(result);
  });
});
