import account from 'frontend-ipdc-enrichment/utils/account';
import { module, test } from 'qunit';

module('Unit | Utility | account', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = account();
    assert.ok(result);
  });
});
