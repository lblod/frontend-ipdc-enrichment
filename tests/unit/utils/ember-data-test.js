import emberData from 'frontend-ipdc-enrichment/utils/ember-data';
import { module, test } from 'qunit';

module('Unit | Utility | ember-data', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = emberData();
    assert.ok(result);
  });
});
